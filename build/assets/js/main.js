(function () { 	function onReady(fn) { 		if (document.readyState !== 'loading') return fn(); 		document.addEventListener('DOMContentLoaded', fn); 	} 	function setupLoader() { 		const loader = document.querySelector('.site-loader'); 		window.addEventListener('load', function () { 			if (!loader) return; 			loader.classList.add('hidden'); 			setTimeout(function () { 				if (loader && loader.parentNode) loader.parentNode.removeChild(loader); 			}, 500); 		}); 	} 	function setupAnchorScroll() { 		document.body.addEventListener('click', function (e) { 			const link = e.target.closest('a'); 			if (!link) return; 			const href = link.getAttribute('href'); 			if (!href || href.charAt(0) !== '#') return; 			const target = document.querySelector(href); 			if (!target) return; 			e.preventDefault(); 			target.scrollIntoView({ behavior: 'smooth', block: 'start' }); 			history.replaceState(null, '', href); 		}); 	} 	function setupMobileNav() { 		const hamburger = document.querySelector('.hamburger'); 		const mobileMenu = document.getElementById('mobile-menu'); 		if (!hamburger || !mobileMenu) return; 		hamburger.addEventListener('click', function () { 			const isOpen = hamburger.getAttribute('aria-expanded') === 'true'; 			hamburger.setAttribute('aria-expanded', String(!isOpen)); 			if (isOpen) { 				mobileMenu.setAttribute('hidden', ''); 				hamburger.classList.remove('open'); 				return; 			} 			mobileMenu.removeAttribute('hidden'); 			hamburger.classList.add('open'); 		}); 	} 	function setupNavScrollState() { 		const nav = document.querySelector('.site-nav'); 		if (!nav) return; 		function onScroll() { 			const y = window.scrollY || window.pageYOffset; 			nav.classList.toggle('scrolled', y > 10); 		} 		window.addEventListener('scroll', onScroll, { passive: true }); 		onScroll(); 	} 	function setupSkillCardVars() { 		const cards = document.querySelectorAll('.skill-card[data-i]'); 		if (!cards || !cards.length) return; 		cards.forEach(function (c) { 			const idx = c.getAttribute('data-i') || '0'; 			c.style.setProperty('--i', idx); 		}); 	} 	onReady(function () { 		setupLoader(); 		setupAnchorScroll(); 		setupMobileNav(); 		setupNavScrollState(); 		setupSkillCardVars(); 	}); })(); 